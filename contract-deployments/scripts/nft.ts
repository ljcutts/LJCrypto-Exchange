// const { ethers } = require("hardhat");
// require("dotenv").config({ path: ".env" });

// async function main() {
//   const LJCryptoNFTCollection = await ethers.getContractFactory(
//     "LJCryptoNFTCollection"
//   );
//   const lJCryptoNFTCollection = await LJCryptoNFTCollection.deploy();

//   await lJCryptoNFTCollection.deployed();

//   console.log(
//     "LJCryptoNFTCollection deployed to:",
//     lJCryptoNFTCollection.address
//   );

// }
// // We recommend this pattern to be able to use async/await everywhere
// // and properly handle errors.
// main().catch((error) => {
//   console.error(error);
//   process.exitCode = 1;
// })
